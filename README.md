# Monorepo

El projecte esta separat en:

- `backend/`: API REST (PHP + Doctrine)
- `frontend/`: client SPA (Laravel)

## Backend

Veure `backend/README.md`.

## Frontend

Veure `frontend/README.md`.
